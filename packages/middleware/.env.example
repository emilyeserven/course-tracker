# PostgreSQL connection string
DATABASE_URL=postgres://postgres:password@localhost:5432/coursetracker

# Anthropic API key — required for the LLM Assist feature on the resource
# modules page. Without it, the /api/resources/:id/suggest-modules endpoint
# returns 503. Get one at https://console.anthropic.com/.
ANTHROPIC_API_KEY=
